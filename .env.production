# Base URL of the Drupal backend's JSON API, consumed at build time by Vite
# (src/config/api.ts). Update this single value whenever the backend moves.
# Expected to end in /api — ASSETS_BASE strips that suffix for image paths.
VITE_API_BASE_URL=https://admin.flywaysglobal.com/api
